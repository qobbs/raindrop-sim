//my variables 
    //generates random slots for the drops to be in
let randX = Math.floor((Math.random() * 500) + 1);
let z;
    //creates the start and end of the canvas
let start = 1;
let end = 1;

//this creates my canvas
function setup() {
    createCanvas(500, 500);
    frameRate(50);
}//end of setup func

//this generates raindrops
function draw(){
    //the sky (background)
    background(8, 23, 155);
    Text("💧", randX, 0);
    //this will place random drops on the canvas
        
    }//end of if
}//end of draw func